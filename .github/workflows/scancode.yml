name: learn-github-actions
on:
  workflow_dispatch:
  push:
    tags:
      - 'v*.0'
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: cd ..
      - run: cd ..    
      - run: ./ScanCode.txt
